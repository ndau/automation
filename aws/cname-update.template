{
  "Comment": "Create a DNS record to point the subdomain to the cluster",
  "Changes": [
    {
      "Action": "UPSERT",
      "ResourceRecordSet": {
        "Name": "PARENT_CLUSTER",
        "Type": "CNAME",
        "TTL": 300,
        "ResourceRecords": [
          {
            "Value": "ELB_ADDRESS"
          }
        ]
      }
    }
  ]
}
