apiVersion: v1
kind: Service
metadata:
  name: {{ template "ndau.fullname" . }}-ndauapi-service
  labels:
    app: {{ template "ndau.name" . }}
    chart: {{ template "ndau.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.ndauapi.service.type }}
  ports:
    - port: 80
      targetPort: {{ .Values.ndauapi.port }}
      protocol: TCP
      name: http
  selector:
    app: {{ template "ndau.name" . }}-ndauapi
    release: {{ .Release.Name }}
